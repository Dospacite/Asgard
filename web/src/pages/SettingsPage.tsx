import { useQuery } from '@tanstack/react-query'
import { Check, Clipboard, CloudCog, KeyRound, LockKeyhole, Network, ServerCog } from 'lucide-react'
import { useState } from 'react'
import { api } from '../api'
import { Button, ErrorState, formatBytes, PageHeader, Skeleton, Status } from '../components/ui'
import type { Disk, Host } from '../types'

type System={host:Host;disk:Disk;goVersion:string;publicUrl:string;domain:string;timezone:string;mcpUrl:string;backupPath:string}
export default function SettingsPage(){const query=useQuery({queryKey:['system'],queryFn:()=>api.get<System>('/system')});const [copied,setCopied]=useState(false);if(query.isPending)return <div className="page"><PageHeader title="Settings"/><Skeleton height={400}/></div>;if(query.isError)return <div className="page"><ErrorState error={query.error}/></div>;const data=query.data;const copy=async()=>{await navigator.clipboard.writeText(data.mcpUrl);setCopied(true);setTimeout(()=>setCopied(false),1500)};return <div className="page"><PageHeader eyebrow="CONTROL PLANE" title="Settings" description="Host identity, security posture, DNS, and agent access."/><div className="settings-grid"><section className="panel settings-summary"><div className="section-heading"><div><p className="eyebrow">HOST</p><h2>Runtime</h2></div><ServerCog/></div><dl><div><dt>Docker</dt><dd><Status value={data.host.available?'healthy':'error'}/> {data.host.dockerVersion}</dd></div><div><dt>Operating system</dt><dd>{data.host.operatingSystem}</dd></div><div><dt>Capacity</dt><dd>{data.host.cpus} vCPU · {formatBytes(data.host.memoryBytes)}</dd></div><div><dt>Disk free</dt><dd>{formatBytes(data.disk.freeBytes)}</dd></div><div><dt>Timezone</dt><dd>{data.timezone}</dd></div></dl></section><section className="panel settings-summary"><div className="section-heading"><div><p className="eyebrow">NETWORK</p><h2>DNS and TLS</h2></div><Network/></div><dl><div><dt>Control plane</dt><dd><a href={data.publicUrl}>{data.publicUrl}</a></dd></div><div><dt>Wildcard</dt><dd><code>*.{data.domain}</code></dd></div><div><dt>Certificates</dt><dd>Traefik · Let’s Encrypt</dd></div><div><dt>HTTP policy</dt><dd>Redirect to HTTPS</dd></div></dl></section></div><section className="agent-panel"><div className="agent-panel__copy"><span className="agent-icon"><CloudCog/></span><p className="eyebrow">AGENT ACCESS</p><h2>Let Codex operate Asgard</h2><p>The MCP endpoint uses OAuth 2.1, PKCE, resource-bound tokens, exact scopes, and a confirmation token for every deletion.</p><div className="scope-row"><span>read</span><span>operate</span><span>deploy</span><span>configure</span><span>backup</span><span>delete</span></div></div><div className="agent-config"><label>MCP endpoint</label><div className="copy-field"><code>{data.mcpUrl}</code><Button variant="secondary" onClick={copy}>{copied?<Check/>:<Clipboard/>}{copied?'Copied':'Copy'}</Button></div><ol><li><KeyRound/><span><strong>Install the Asgard plugin</strong><small>It includes deployment and development skills.</small></span></li><li><LockKeyhole/><span><strong>Authorize in your browser</strong><small>Review requested scopes before approval.</small></span></li><li><CloudCog/><span><strong>Ask the agent to inspect first</strong><small>Mutations return durable operation IDs.</small></span></li></ol></div></section><section className="panel security-panel"><div><p className="eyebrow">SECURITY</p><h2>Single-administrator posture</h2></div><ul><li><Check/>Argon2id password hashing</li><li><Check/>15-minute Ed25519 session JWTs</li><li><Check/>Rotating 30-day refresh sessions</li><li><Check/>SameSite Strict, Secure, HttpOnly cookies</li><li><Check/>CSRF protection and login rate limiting</li><li><Check/>No private Git or registry credentials in MVP</li></ul></section></div>}
